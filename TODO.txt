* Display:
  * Extend Image class specific to MonoGame that contains a direct reference to the texture it uses for performance reasons.
* Audio:
  * Apparently, missing OpenAL.dll, so I can't reliably test sound yet...need to fix this
  * Implement usage of AudioParams
* Text:
  * Extend TextFormat class specific to MonoGame that contains a direct reference to the sprite font it uses for performance reasons.
  * Use spriteBatch.GraphicsDevice.scissorRectangle and RasterizerState.ScissorTestEnabled to clip bounds, make sure to reset when done.
  * Use spriteFont.MeasureString to fetch a vector of the width and height. Split words and insert new line where it's too long for the width.
  * Fields:
    * text
	* font
	* alignment
	* width
	* height
	* multiline
	* wordWrap
